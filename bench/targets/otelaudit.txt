# vegeta target file — otelaudit bench, stdout exporter (WI-4yaa.BENCH-5)
# Token placeholder {{TOKEN}} is replaced by harness.sh with a live HS256 JWT.
# GET /bench is the synthetic endpoint wired to noop-upstream in bench-routes.yaml.
GET http://localhost:8120/bench
Authorization: {{TOKEN}}
Content-Type: application/json
